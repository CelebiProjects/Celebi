Installation
================

In order to use Celebi, three components need to be installed or set up:
1. **Celebi package**: The package that manage the repository and provides the command line interface.
2. **Yuki middleware**: The middleware that handles the execution of tasks.
3. **Runner backends**: The execution backends that run the tasks (e.g., reana).

